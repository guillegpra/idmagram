<!DOCTYPE html>
<html lang="en">
    <%- include('./common/head'); %>

    <body>
        <%- include('./common/nav'); %>

        <div class="row px-5">

            <div class="photo col-sm text-center align-self-center">
                <%if (locals.image) { %>
                <img src="<%= image %> " alt="<%= image_name %>" />
                <% } %>
            </div>

            <div class="photo-info col-sm text-center align-self-center">
                username
                date
                likes
                comments
            </div>
        </div>

        <%- include('./common/footer'); %>
    </body>

</html>